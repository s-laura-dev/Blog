/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React Strict Mode for better error checking
  swcMinify: false, // Disable minification for development to see full error messages
};

export default nextConfig;
